/**
 * Empty stub for the user-app `cms/__generated__/configInit` side-import.
 * Wired up via the `resolve.alias` in `vitest.config.ts` so package tests
 * don't need a live config singleton.
 */
export {};
