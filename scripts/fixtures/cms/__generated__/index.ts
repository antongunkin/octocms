/**
 * Fixture stub for standalone octocms development.
 *
 * This file satisfies the `cms/__generated__` TypeScript path alias defined in
 * octocms/tsconfig.json so that `tsc --noEmit` passes when running inside the
 * octocms package directory. It mirrors the shape of the real generated types
 * produced by `npm run types:gen` in the consuming app.
 *
 * Do not import this at runtime — it is only used for type-checking.
 */

export type EntryMap = Record<string, never>;
export type AnyEntry = never;
export const query = undefined as unknown;
