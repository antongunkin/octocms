// Re-export from the shadcn-style ui package so existing callsites keep working.
// The implementation now lives in ui/status-badge.tsx with the v2 token palette.
export { StatusBadge, STATUSES } from './ui/status-badge';
